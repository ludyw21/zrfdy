{
  "name": "txt-processor-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to process txt files from GitHub",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}